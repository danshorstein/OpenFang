"""
OpenFang — LLMs write automations, not be automations.

A fork of OpenClaw that treats the LLM as a strategist and engineer,
writing and maintaining deterministic Python automations instead of
routing every task through a language model.
"""

__version__ = "0.1.0"
